rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Receipts: only authenticated users can access their own receipts
    match /receipts/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
